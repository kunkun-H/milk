<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.milk.mapper.EvaluationImageMapper">

    <insert id="insert">
        insert into evaluation_image(evaluation_id,image,create_time)
        values (#{evaluationId},#{image},#{createTime})
    </insert>
</mapper>
