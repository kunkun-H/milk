<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.milk.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail (name, image, order_id, goods_id, setmeal_id, goods_flavor, number, amount)
            values
        <foreach collection="orderDetails" item="od" separator=",">
            (#{od.name},#{od.image},#{od.orderId},#{od.goodsId},#{od.setmealId},#{od.goodsFlavor},#{od.number},#{od.amount})
        </foreach>
    </insert>
</mapper>
